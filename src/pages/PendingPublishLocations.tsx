import React from 'react';import { Link } from 'react-router-dom';import Header from "../components/layout/header/Header";import Footer from "../components/layout/Footer";import BigCard from "../components/common/cards/BigCard";import CardBody from "../components/common/cards/CardBody";import CardTitleCentered from "../components/common/cards/CardTitleCentered";import pendingPublishLocationsHook from "../hooks/pendingPublishLocationsHook.tsx";const PendingPublishLocations = () => {	const { locations, isLoading, error } = pendingPublishLocationsHook();		if (isLoading) return (		<div className="min-h-screen flex justify-center items-center bg-gray-100">			<span className="loading loading-spinner loading-lg text-primary"></span>		</div>	);		if (error) return (		<div className="min-h-screen flex justify-center items-center bg-gray-100">			<div className="alert alert-error shadow-lg max-w-md">				<div>					<svg xmlns="http://www.w3.org/2000/svg" className="stroke-current flex-shrink-0 h-6 w-6" fill="none" viewBox="0 0 24 24"><path strokeLinecap="round" strokeLinejoin="round" strokeWidth="2" d="M10 14l2-2m0 0l2-2m-2 2l-2-2m2 2l2 2m7-2a9 9 0 11-18 0 9 9 0 0118 0z" /></svg>					<span>{error}</span>				</div>			</div>		</div>	);		return (		<div className="flex flex-col min-h-screen bg-gray-100">			<Header />			<main className="flex-grow container mx-auto px-4 py-8">				<h1 className="text-4xl font-bold text-center mb-12 text-gray-800">Locations Pending Publish</h1>				{locations.map(location => (					<BigCard key={location.id} className="shadow-xl mb-8">						<CardBody>							<CardTitleCentered text={location.title} className="text-xl mb-4" />							<p>Status: {location.locationStatus}</p>							<p>Country: {location.country}</p>							<p>Price per night: ${location.pricePerNight}</p>							<p>Images: {location.documents.filter(doc => doc.documentType === 'IMAGES').length}</p>							<Link to={`/locations/${location.id}/publish`} className="btn btn-primary mt-4">								Manage Images							</Link>						</CardBody>					</BigCard>				))}			</main>			<Footer />		</div>	);};export default PendingPublishLocations;