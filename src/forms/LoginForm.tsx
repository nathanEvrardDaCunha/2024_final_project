import React from 'react';import loginFormHook from "../hooks/loginFormHook";import FormControl from "../components/common/forms/FormControl";import Label from "../components/common/forms/Label";import WarningMessage from "../components/common/WarningMessage";import SubmitButton from "../components/common/forms/SubmitButton";import Input from "../components/common/forms/Input";const LoginForm = () => {	const { formData, errors, isSubmitting, attemptedSubmit, handleChange, handleSubmit } = loginFormHook();		return (		<form onSubmit={handleSubmit}>			<FormControl>				<Label htmlFor="email" text="Email" />				<Input					type="email"					name="email"					value={formData.email}					onChange={handleChange}					placeholder="johndoe@gmail.com"				/>				{attemptedSubmit && errors.email && <WarningMessage message={errors.email} />}			</FormControl>			<FormControl>				<Label htmlFor="password" text="Password" />				<Input					type="password"					name="password"					value={formData.password}					onChange={handleChange}					placeholder="Enter your password"				/>				{attemptedSubmit && errors.password && <WarningMessage message={errors.password} />}			</FormControl>			<SubmitButton text="Login" disabled={isSubmitting} />		</form>	);};export default LoginForm;