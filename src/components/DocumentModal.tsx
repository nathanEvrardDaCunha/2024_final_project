import React from 'react';import { PublishedDocument } from '../types/adminLocationType';interface DocumentModalProps {	documents: PublishedDocument[];	isLoading: boolean;	onClose: () => void;}const DocumentModal: React.FC<DocumentModalProps> = ({ documents, isLoading, onClose }) => {	return (		<div className="fixed inset-0 bg-black bg-opacity-50 flex justify-center items-center z-50">			<div className="bg-white p-6 rounded-lg max-w-2xl w-full max-h-[80vh] overflow-y-auto">				<h2 className="text-2xl font-bold mb-4">Location Documents</h2>				{isLoading ? (					<p>Loading documents...</p>				) : documents.length === 0 ? (					<p>No PAPER_ASSET documents available for this location.</p>				) : (					<ul className="space-y-4">						{documents.map((doc) => (							<li key={doc.id} className="border p-4 rounded-lg">								<h3 className="font-semibold">{doc.filename}</h3>								<p>Type: {doc.documentType}</p>								<a									href={`data:application/octet-stream;base64,${doc.file}`}									download={doc.filename}									className="btn btn-primary mt-2"								>									Download Document								</a>							</li>						))}					</ul>				)}				<button					className="mt-4 btn btn-secondary"					onClick={onClose}				>					Close				</button>			</div>		</div>	);};export default DocumentModal;