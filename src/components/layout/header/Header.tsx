import React from 'react';import Logo from './Logo';import NavItems from './NavItems';import BurgerMenu from './BurgerMenu';import {headerHook} from "../../../hooks/headerHook.tsx";const Header: React.FC = () => {	const { isLoggedIn, handleDisconnect } = headerHook();		return (		<div className="navbar bg-base-100">			<div className="navbar-start">				<Logo />			</div>			<div className="navbar-end">				<div className="hidden lg:flex">					<ul className="menu menu-horizontal px-1">						<NavItems isLoggedIn={isLoggedIn} onDisconnect={handleDisconnect} />					</ul>				</div>				<BurgerMenu isLoggedIn={isLoggedIn} onDisconnect={handleDisconnect} />			</div>		</div>	);};export default Header;