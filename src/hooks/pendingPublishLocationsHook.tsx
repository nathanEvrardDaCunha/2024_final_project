import { useState, useEffect } from 'react';import { Location } from '../types/adminLocationType';import {getLocationsPendingPublish} from "./adminLocationService.tsx";const pendingPublishLocationsHook = () => {	const [locations, setLocations] = useState<Location[]>([]);	const [isLoading, setIsLoading] = useState(true);	const [error, setError] = useState<string | null>(null);		useEffect(() => {		fetchLocations();	}, []);		const fetchLocations = async () => {		try {			const fetchedLocations = await getLocationsPendingPublish();			setLocations(fetchedLocations);		} catch (err) {			setError('Failed to fetch locations');		} finally {			setIsLoading(false);		}	};		return { locations, isLoading, error };};export default pendingPublishLocationsHook;