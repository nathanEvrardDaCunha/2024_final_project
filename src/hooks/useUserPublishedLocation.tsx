import { useState, useEffect } from 'react';import { PublishedLocation } from "../types/adminLocationType";import {getUserPublishedLocations} from "./adminLocationService.tsx";const useUserPublishedLocations = () => {	const [locations, setLocations] = useState<PublishedLocation[]>([]);	const [isLoading, setIsLoading] = useState(true);	const [error, setError] = useState<string | null>(null);		useEffect(() => {		const fetchLocations = async () => {			try {				const fetchedLocations = await getUserPublishedLocations();				setLocations(fetchedLocations);			} catch (err) {				setError('Failed to fetch user\'s published locations');			} finally {				setIsLoading(false);			}		};				fetchLocations();	}, []);		return { locations, isLoading, error };};export default useUserPublishedLocations;