import { useState, useEffect } from 'react';import {getPublishedLocations} from "./adminLocationService.tsx";import {PublishedLocation} from "../types/adminLocationType.tsx";const usePublishedLocations = () => {	const [locations, setLocations] = useState<PublishedLocation[]>([]);	const [isLoading, setIsLoading] = useState(true);	const [error, setError] = useState<string | null>(null);		useEffect(() => {		const fetchLocations = async () => {			try {				const fetchedLocations = await getPublishedLocations();				setLocations(fetchedLocations);			} catch (err) {				setError('Failed to fetch published locations');			} finally {				setIsLoading(false);			}		};				fetchLocations();	}, []);		return { locations, isLoading, error };};export default usePublishedLocations;